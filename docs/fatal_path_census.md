# Fatal-Path Census — Zero-Fatal Ladder (build directive #5)

Line numbers reference handler.py @ 6d093fa (pre-ladder). Verdicts:
RETRYABLE / DEGRADABLE-SAFE-EDIT / DEGRADABLE-RENDER-LADDER / INPUT-REJECT /
IRREDUCIBLE-INFRA (out of fallback scope by directive — retry only).

All line numbers = `handler.py` @ HEAD `6d093fa` (branch `broll-picker-vision`). "Outcome" = what `classify_error` (17893) returns when the raise reaches the outer `except` at 22115 → `classified = classify_error(e)` at 22123 (durable `failed` write 22126, structured return 22134).

| # | Stage | Class or raise | Trigger (file:line + condition) | Today's outcome | Verdict |
|---|-------|----------------|--------------------------------|-----------------|---------|
| 1 | intake | missing required fields | handler.py:19065-19068 — any of job_id/video_url/vibe/user_id/upload_url absent → `return {"error": ...}` (no classify, no durable write) | raw error dict; job never starts | INPUT-REJECT (pre-spend) |
| 2 | intake | tier/concurrency gate | handler.py:19081-19089 — `check_concurrency_gate` (def 1281, FAIL-OPEN on Supabase trouble) returns reject dict for free-tier concurrent job | error dict w/ tier message; no classify | INPUT-REJECT (policy, pre-spend) |
| 3 | intake | re-edit mode preconditions | handler.py:19226-19233 — render_only w/o edit_plan; tweak/guided_redraft w/o plan+change_request; reinterpret w/o change_request → `return {"error": ...}` | raw error dict | INPUT-REJECT (pre-spend) |
| 4 | download | bad source URL | handler.py:19296-19297 — `_parse_aws_s3_url` fails → RuntimeError "Not a valid AWS S3 URL" | S3_GENERIC (retryable) — mislabel: retry can't fix a malformed URL | INPUT-REJECT (dispatch bug) |
| 5 | download | S3 client not initialized | handler.py:19298-19299 — `_aws_s3_client` None → RuntimeError mentioning "AWS S3 client" | S3_GENERIC (retryable) | IRREDUCIBLE-INFRA |
| 6 | download | UPLOAD_STALLED | handler.py:19531-19539 — HEAD poll deadline exceeded (deadline is start+1800s at 19485; message text says "300s" — stale) → RuntimeError "UPLOAD_STALLED: Source video did not arrive on S3..." | UPLOAD_STALLED (retryable) — literal matched at 17960 before the "did not arrive on S3" UPLOAD_TIMEOUT check 17966 | RETRYABLE |
| 7 | download | source download failure | handler.py:19550 — `download_file` boto ClientError propagates | S3_ACCESS (17972) or S3_GENERIC (17978) — retryable | RETRYABLE |
| 8 | download (multi-input) | multi-input ingest | call handler.py:19427 (not try-wrapped) → `_download_and_concat_sources` 13310: client None 13337 (INFRA), bad URL 13342 (INPUT-REJECT), per-source `download_file` 13344 (RETRYABLE), concat via `run_ffmpeg` 13388 → "FFmpeg failed:" 13299 | S3_GENERIC / RENDER_FFMPEG (all retryable) | mixed — see per-site tags |
| 9 | input gate | NOT_TALKING_HEAD fast check | handler.py:19716-19733 — `_quick_face_check` (def 18982): ≥5 samples AND face_ratio<0.25 → RuntimeError "NOT_TALKING_HEAD: face in only ..%" (19729); non-RuntimeError checker errors swallowed 19738-19746 | NOT_TALKING_HEAD (retryable=False, requires_new_video=True) | INPUT-REJECT (early ✓, ~2-3s post-download) |
| 10 | input gate | NOT_TALKING_HEAD full gate | handler.py:20714-20723 — inside recipe task `_do_edit_recipe_overlapped`, BEFORE the Gemini call (call starts 20728+): face_samples≥8 AND ratio<0.20 AND duration≥15s AND words<10 → RuntimeError 20717; surfaces via `future_edit.result()` 20918 | NOT_TALKING_HEAD (new_video) | INPUT-REJECT (post transcribe+faces, pre-Gemini — acceptable 2nd layer) |
| 11 | analyze | corrupt source / no video stream | handler.py:13410 — `analyze_source_video` (def 13392) RuntimeError "No video stream found in source"; runs as `_do_normalize` future, consumed 20549 / 21294 | INVALID_FORMAT (18000, new_video) | INPUT-REJECT (LATE-ish: mega-parallel already dispatched Gemini proxy + Deepgram; see (c)) |
| 12 | analyze | Deepgram audio prep ffmpeg | handler.py:19754-19762 — `_do_transcribe`: opus extract rc≠0 → "FFmpeg audio extraction failed" 19760; empty file → 19762 | RENDER_FFMPEG (18070, retryable) — misclass for an unreadable-audio input | INPUT-REJECT |
| 13 | transcribe | Deepgram terminal failure | handler.py:2660-2677 — 3 attempts w/ backoff (retriable-only, 2671) then RuntimeError "Deepgram transcription failed after 3 attempts" 2677 | TRANSCRIPTION (18020, retryable=False, new_video=True) — an API outage is told "use a different clip" | RETRYABLE (infra outage) — today mislabeled INPUT-REJECT |
| 14 | analyze probes | loudness / vocal-emphasis ffmpeg | handler.py:2696-2697, 2706-2707 (`measure_source_loudness`), 2748-2750 (`detect_vocal_emphasis`) — raise on rc≠0/no data; futures consumed in recipe path, not fail-open | RENDER_FFMPEG (retryable) | INPUT-REJECT (unreadable audio) |
| 15 | analyze | Gemini proxy encode failure | handler.py:19845-19865 — on-server 480p encode rc≠0 → "Gemini proxy encode failed" 19858/19865; face-detect task also blocks on this future (20822) so it propagates even if recipe path changed | EDITOR_GENERIC (18040, retryable) — corrupt-source rejection in disguise | INPUT-REJECT (misrouted) |
| 16 | recipe | Gemini transport/API errors | call site handler.py:7669 `_call_gemini_post_cuts` — OUTSIDE the repair try (opens 7805; note 10655-10656). API msgs classify: " 429"/quota→RATE_LIMIT 17992; DEADLINE_EXCEEDED→EDITOR_TIMEOUT 18028; else "Gemini"→EDITOR_GENERIC 18040 | retryable in all three classes | RETRYABLE |
| 17 | recipe | post-cuts degeneration exhaustion | handler.py:6705-6814 — 2 attempts; degenerate = empty (6766-6767), >16K output tokens (6768-6770), unparseable JSON (6771-6775) → RuntimeError "Gemini post-cuts-call degenerate after retry" 6814 | EDITOR_GENERIC (retryable) | RETRYABLE |
| 18 | recipe | no video part wired | handler.py:7300 — neither inline_video_bytes nor gemini_file → RuntimeError "No video data provided" | EMPTY_UPLOAD (18012, retryable) — actually internal wiring, not the user's upload | IRREDUCIBLE-INFRA |
| 19 | recipe | repair-net exhaustion → RECIPE_INVALID | loop handler.py:7664, net catch 10654 `except (ValueError, RuntimeError)`, exhaustion 10659-10662 → `RecipeInvalidError("RECIPE_INVALID after N attempt(s)...")` (class def 17883). Default 1 re-ask (7656), budget gate `duration*3+180 ≤ 900` (7662). ENCLOSES every post-parse validator 7805-10653: cut_refinements 7985-7994; cut application 8291 `build_clips_from_words` incl. its overlap invariant 14396-14403; **"removed all words — no clips remain" 8513-8514**; missing remove_words 8516-8520; transitions 8793-8802; tight_cut_overlays 8955-8972; sfx 9210-9256; captions/MG 9417-9548; emphasis 9615-9868; text_overlays 10415-10452; **final_cuts transition_out RuntimeError 10570-10576**; **clip-speed ValueError 10588-10597**; every "source_start/chronological"-worded validator | RECIPE_INVALID (17938, retryable) — sentinel matched FIRST, so EMPTY_EDIT/PLAN_INVALID/etc. never see these | DEGRADABLE-SAFE-EDIT (mechanical-cuts-only plan is derivable with zero Gemini); empty-transcript sub-case = INPUT-REJECT |
| 20 | recipe→render seam | zero-word "No speech detected" | handler.py:21365-21374 — `len(_dg_words)==0 and mode != "render_only"` → RuntimeError "No speech detected in source (Deepgram returned 0 words)..." | TRANSCRIPTION (matches "Deepgram" 18020; new_video) — but see (a): effectively SHADOWED by row 19 | INPUT-REJECT (should run pre-Gemini) |
| 21 | tweak path | plan-diff failure | handler.py:19570 `generate_plan_diff` (not try-wrapped): preconditions 10692/10694; `extract_json` raises 2816 "Empty Gemini response" / 2840 "Could not extract valid JSON from Gemini response" | EDITOR_PARSE (18034) or EDITOR_GENERIC / UNKNOWN — retryable | RETRYABLE (fallback-to-reinterpret would be DEGRADABLE-SAFE-EDIT) |
| 22 | broll | Pexels fetch/verify | `prefetch_and_verify_broll` 12706: future exceptions logged + entry dropped 12736-12746; None results dropped 12748-12757 — NEVER terminal | fail-open (BROLL class 18084 has no live feeder) | n/a (already degradable) |
| 23 | render input | Pydantic schema validation | `_validate_and_write_render_input` 112-135, raise 130-133 "[label] render input failed schema validation against {cls}:\n{ve}"; called at 16642 (base comp) + 16754 (overlay comp) inside `render_multi_clip` (def 14554, called 21434) | PLAN_VALIDATION (18062 via lowercase "validation error"; retryable) | DEGRADABLE-SAFE-EDIT (strip offending decoration, re-validate) |
| 24 | render (audio) | source-audio extraction | handler.py:13712-13718 ("Source audio extraction failed", build_per_cut_audio def 13649) and 14766-14772 (boundary-refinement extract) | stderr-dependent: usually UNKNOWN [error-fallback 18094], retryable | IRREDUCIBLE-INFRA |
| 25 | render (derivation) | internal invariants | 14519 "project_source_time_to_output requires clip_time_maps"; 15243-15252 TCO duration registry missing entry; 15795-15870 pipeline-integrity postconditions (transition/MG/text-overlay counts) | UNKNOWN (retryable) — no trigger substrings | IRREDUCIBLE-INFRA (deterministic code/config bugs; retry re-fails) |
| 26 | render (captions) | caption projection empty | handler.py:15484 "caption_position_segments projection produced no output frames" | UNKNOWN (retryable) | DEGRADABLE-RENDER-LADDER (drop caption repositioning) |
| 27 | render (Remotion) | Remotion subprocess failure | handler.py:16886-16894 — rc≠0 → "[label] Remotion render failed (rc=...)" | RENDER_REMOTION (18076, retryable) | DEGRADABLE-RENDER-LADDER (retry once, then re-render with reduced overlay load) |
| 28 | render (chunks) | chunk missing / concat / composite | micro missing 17268, micro concat 17278-17284, overlay missing 17311-17315 + 17467 + 17511, "[composite-K] ffmpeg failed" 17334-17340, overlay concat 17481-17485, PromptlyOverlay/Micro output missing 17514/17518 | "ffmpeg"-worded ones → RENDER_FFMPEG; "missing/invalid" ones → UNKNOWN (all retryable) | RETRYABLE |
| 29 | render (audio mix) | per-cut audio / post-processing | 17361 "Per-cut audio pipeline produced no output"; 17405 "Audio post-processing failed: {stderr}" | UNKNOWN or RENDER_FFMPEG (stderr-dependent), retryable | RETRYABLE |
| 30 | render (final mux) | final composite / concat+mux | 17564 "Final composite failed"; 17645-17652 "Final concat+mux failed (rc=...)" | stderr-dependent RENDER_FFMPEG/UNKNOWN, retryable | RETRYABLE |
| 31 | render (post-mux QA) | A/V drift > 30ms | handler.py:17675-17702 — `abs(_av_drift_ms) > 30.0` → RuntimeError "A/V drift +X.XXms exceeds 30ms target" 17696 | UNKNOWN [error-fallback] (retryable) — "A/V drift" has no classify entry | IRREDUCIBLE-INFRA (deterministic cut-math bug — retry reproduces) |
| 32 | render output check | invalid/short output | 21451-21452 "Main render produced invalid output" (<100KB); 21474-21475 "Main render output too short" (<1.0s); 21540-21541 "Final output is invalid" (`validate_output` 13257) | UNKNOWN (retryable) | RETRYABLE |
| 33 | QA (premium scenes) | judge + recovery | 21552-21645 — judge fail → perturb+regen+re-render (≤2 attempts, cost + `render_elapsed > _RERENDER_HEADROOM_S` 21588/6370 gates) → DEGRADE drops scenes + one re-render 21589-21612; whole block fail-OPEN 21640-21645 | never terminal by itself | n/a — the existing DEGRADABLE-RENDER-LADDER precedent |
| 34 | upload | main MP4 upload | `_upload_main` 21651: no upload_url 21665, AWS client 21669, Supabase client 21675, unparseable URL 21678-21681 (all → UNKNOWN or S3_GENERIC); `upload_file` boto errors 21683-21687 → S3_ACCESS/S3_GENERIC; surfaces via `f_upload.result()` 21938 | retryable | config raises = IRREDUCIBLE-INFRA; boto = RETRYABLE |
| 35 | upload | cover-frame scorer | `_extract_and_upload_cover` 21701 — `select_best_thumbnail_frame` (def 11699) errors propagate BY DESIGN ("No fallback — whole job fails" 21726-21727) via `f_cover.result()` 21939; thumbnail UPLOAD itself is non-fatal 21752-21763 | message-dependent (Gemini-worded → EDITOR_GENERIC; else UNKNOWN) | DEGRADABLE-RENDER-LADDER (ship with fallback frame) |
| 36 | upload | HLS ladder (required by design 21774-21776) | `_upload_hls` 21777: client missing 21778-21782 ("S3"→S3_GENERIC); non-AWS upload_url 21783-21788; encode rc≠0 21858-21862 "HLS encode failed"; zero files 21887-21888; missing public_url 21893-21897; segment `upload_file` 21882 boto errors; via `f_hls.result()` 21940 | S3_GENERIC or UNKNOWN, retryable | DEGRADABLE-RENDER-LADDER (ship MP4, backfill HLS) |
| 37 | exports | extra aspect-ratio formats | 21979-21988 — per-format failures logged "(non-fatal)" and skipped | fail-open, never terminal | n/a |
| 38 | park (not failure) | needs_clarification / ask-back | tweak: 19578-19590 returns `{"status":"needs_clarification"}` + durable needs_input; premium input-quality ask: 21097 → `_emit_ask_and_suspend` 18309-18328 returns `{"status":"needs_input", ask}` | intentional terminal park, resumable via mode=resume_ask (19187-19223, fail-open partial_state read 19189-19192) | n/a |
| 39 | wall-clock | Modal 900s function timeout | referenced in budget math 7662 / 7797 (`≤ 900.0`); no in-process handler — container killed | no classify, no durable failed write — client sees stall | IRREDUCIBLE-INFRA (the elapsed-budget hook in (b) is the remedy point) |

DEAD classify_error entries (no live raise site feeds them): UPLOAD_NEVER_STARTED (17954 — precheck removed, see 19505-19529); UPLOAD_TIMEOUT (17966 — its substrings only occur inside the UPLOAD_STALLED message, which 17960 matches first); WRONG_ORIENTATION "Landscape video" (18006 — landscape is auto-reframed at ingest, no raise anywhere); EMPTY_EDIT (18048 — "removed all words"/"no clips remain" only raised at 8514 inside the repair net → always re-wrapped RECIPE_INVALID; "missing cuts array" has no site); PLAN_INVALID (18056 — every source_start/source_end/chronological validator lives inside the repair net); "Pre-split mismatch" (18070 — no site); BROLL (18084 — fetch path is fail-open).

## Key findings acted on in this PR

- **Zero-word transcript** (was: proceeded through TWO Gemini calls, died as
  retryable RECIPE_INVALID): now rejects pre-Gemini with the honest NO_SPEECH
  class (requires_new_video). PROCEEDS through the full (double) Gemini recipe spend, then dies as RECIPE_INVALID — the dedicated 0-word reject is unreachable. Exact chain @ HEAD 6d093fa: (1) the talking-head gate (handler.py:20714-20716) only rejects when face_low AND speech_low both hold (≥8 face samples + ratio<0.20 + duration≥15s + words<10), so a silent-but-faced video passes; (2) handler.py:20726-20727 logs "[pipeline] W

- **Job clock for the budget shed:** `_pipeline_start` — handler.py:19282 (`_pipeline_start = time.time()`), assigned directly in handler() scope after the intake/tier/mode returns and before download; still in scope at the render stage (render_multi_clip call at 21434) and already used there-abouts for `_timings["t

- **Late detectors found:** NOT all pre-spend. Early (pre-recipe, cheap): tier gate 19081, field/mode returns 19065/19226, S3 URL parse 19296, UPLOAD_STALLED 19531, multi-input URL checks 13342, NOT_TALKING_HEAD fast check 19716-19733 (~2-3s post-download). LATE detectors: (1) WORST — zero-speech: the intended reject at 21365-21374 fires only after `future_edit.result()` (20918) and is in practice shadowed by the repair-net path, so a silent-with-face video burns TWO full Gemini Pro editorial calls before dying, and dies with the wrong class (retryable RECIPE_INVALID instead of requires_new_video TRANSCRIPTION). (2) Corrupt-source INVALID_FORMAT: "No video stream found in source" (13410) is raised in the `_do_normalize` future and consumed at 20549/21294 — by then the mega-parallel phase has already dispatched Deepgr

## Coverage statement (what the ladder covers vs excepts)

- Rows verdicted DEGRADABLE-SAFE-EDIT → covered by build_safe_recipe at the
  repair-net exhaustion + transport-exhaustion triggers (EMPTY_EDIT's raise
  sites live INSIDE the net's span, so they reach the safe edit via
  exhaustion).
- Rows verdicted DEGRADABLE-RENDER-LADDER / render-stage RETRYABLE → covered
  by _render_degrade_ladder (retry → stripped → RENDER_FATAL); the post-mux
  |v−a| guard raises inside the wrapped callable and routes through the rungs.
- INPUT-REJECT rows → fast-path audit: NO_SPEECH moved pre-Gemini;
  malformed-URL and proxy-encode-failure reclassified honest
  (INVALID_SOURCE_URL / INVALID_FORMAT, requires_new_video).
- EXPLICITLY EXCEPTED (called out, not silently left): render-input Pydantic
  validation (row 23) engages the ladder's rungs but a deterministic schema
  bug will surface as RENDER_FATAL rather than a safe-edit rebuild (a
  recipe-stage rebuild from inside the render stage would need re-entering
  generate_edit_gemini — ledgered); render-derivation invariants (row 25) and
  audio-extraction failures (row 24) are IRREDUCIBLE (deterministic code/infra
  — the ladder retries them, honestly surfacing as RENDER_FATAL); plan-diff
  failures on the tweak rail (row 21) remain RETRYABLE (fallback-to-
  reinterpret ledgered); infra rows (5, 6, 7, 18, 28-30 infra-shaped causes)
  stay retry-only per the directive.
